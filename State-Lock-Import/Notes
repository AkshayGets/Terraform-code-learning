First add the resource section to accomodate the left over resource and then run the import command post which run the destroy command to successfully destroy all the resources.
terraform import command - terraform import docker_container.foo $(docker inspect -f {{.ID}} foo)

